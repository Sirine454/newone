PROCEDURE simple_sort(VAR tab : ARRAY_OF INTEGER)
VAR
   i,j,n,value : INTEGER;
BEGIN
   n := tab.length;
   FOR i FROM 0 TO n- 1 STEP 1  DO
   value=tab[i]
   j=i;
   WHILE (j > 0) && (tab[j − 1] > value) DO
      arr[j] = arr[j − 1];
      j = j − 1;
   END_WHILE
   arr[j] = value;
   END_FOR
END